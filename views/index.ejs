<!DOCTYPE HTML>
<html>
	<head>
		<title>Index</title>
		<link rel="stylesheet" type="text/css" href="/styles.css">
	</head>
	<body>
		<h1>Here is a list of all the mongooses</h1>
		<a href="/mongooses/new">Create a new mongoose</a>

		<% for (var x in errors) { %>
		<h3><%= errors[x].message %></h3>

		<% } %>
		
			<% for (var x in mongooses){ %>
			<div class="box">
				<p>Mongoose Name: <a href="/mongooses/<%=mongooses[x].id%>"><%= mongooses[x].name %></a></p>
				<p class="under"> Mongoose Age: <%= mongooses[x].age %></p>
			</div>
			<% } %>
		
	</body>
</html>